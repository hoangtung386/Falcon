# Project-wide configuration
# Code formatting is handled by black + isort (see .pre-commit-config.yaml)

[metadata]
license_files = LICENSE
description_file = README.md

[tool:pytest]
norecursedirs =
    .git
    dist
    build
addopts =
    --doctest-modules
    --durations=25
    --color=yes
